  <div class="text-center">
    <h1 class="mb-3"> 🕺 <%= @cocktail.name.capitalize %> 🎉</h1>
    <%= cl_image_tag @cocktail.photo, height: 500, width: 1100, crop: :fit %>

  </div>
<hr>



  <div class="d-flex justify-content-beetween">
    <h2> Ingredient </h2>
  </div>




  <div class="mt-3 mb-3">
    <% @cocktail.doses.each do |dose| %>
    <li class="list-group-item d-flex justify-content-between align-items-center">
      <span><strong>Quantity :</strong> <%= dose.description %></span><span class="badge-primary badge-pill"><%= dose.ingredient.name %></span>
      <h2>
       <%= link_to "🗑", dose_path(dose),
        method: :delete,
        data: { confirm: "Are you sure that you want to delete it ?" } %>
      </h2>

    </li>
    <% end %>
  </div>

  <div class="mt-5">
    <hr>
  </div>

  <div class="mt-3 mb-5">
    <h3> Add an Ingredient </h3>
    <%= simple_form_for [@cocktail, @dose] do |f| %>
      <%= f.input :description%>
      <%= f.association :ingredient %>

      <%= f.submit "add an ingredient", class: "btn btn-primary" %>
    <% end %>
  </div>

